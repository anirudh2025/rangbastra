<header class="signature-header">
  <span class="eyebrow">The Rangbastra Difference</span>

  <h2>
    <span class="title-intro">Couture Isn't Chosen</span>
    <span class="title-statement">It Is Recognized</span>
  </h2>

  <p>
    We do not begin with inventory. We begin with the woman, the moment and the
    memory the garment must hold.
  </p>
</header>

<style>
  .signature-header {
    display: grid;
    gap: var(--space-5);
    max-width: var(--measure-md);
  }

  .eyebrow {
    color: var(--cocoa-400);
    font-size: var(--caption);
    font-weight: var(--weight-semibold);
    letter-spacing: var(--tracking-label);
    line-height: var(--leading-caption);
    text-transform: uppercase;
  }

  h2 {
    display: grid;
    gap: var(--space-2);
    max-width: min(100%, 54rem);
    color: var(--text-primary);
    font-family: var(--font-display);
    font-weight: var(--weight-light);
    text-wrap: balance;
  }

  .title-intro {
    color: var(--text-primary);
    font-size: var(--section-heading-intro);
    letter-spacing: var(--tracking-heading);
    line-height: 1.18;
    white-space: nowrap;
  }

  .title-statement {
    background: var(--gradient-brand-text);
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
    -webkit-text-fill-color: transparent;
    font-size: var(--section-heading-statement);
    letter-spacing: var(--tracking-heading);
    line-height: 1.12;
    padding-bottom: .08em;
    white-space: nowrap;
  }

  p {
    max-width: var(--measure-md);
    color: var(--text-secondary);
    font-size: var(--body-lg);
    line-height: var(--leading-body);
    text-wrap: pretty;
  }

  @media (max-width: 767px) {
    .signature-header {
      gap: var(--space-4);
    }

    .title-intro {
      font-size: var(--section-heading-intro-mobile);
    }

    .title-statement {
      font-size: var(--section-heading-statement-mobile);
    }

    p {
      font-size: var(--body-md);
    }
  }
</style>
