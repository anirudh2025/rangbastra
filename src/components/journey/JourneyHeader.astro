<section class="journey-header">

    <span class="eyebrow">

        THE EXPERIENCE

    </span>

    <h2 class="title">

        Your Journey To

        <span>

            Timeless Couture

        </span>

    </h2>

    <div class="divider"></div>

    <p class="description">

        From the very first conversation to the final fitting,
        every creation is thoughtfully designed, meticulously
        handcrafted and tailored exclusively for your celebration.

    </p>

</section>

<style>

.journey-header{

    /*
    =====================================================
    LOCAL TOKENS
    =====================================================
    */

    --journey-header-gap:
        clamp(
            .9rem,
            1.5vw,
            1.5rem
        );

    --journey-title-width:
        18ch;

    --journey-description-width:
        58ch;

    display:flex;

    flex-direction:column;

    align-items:center;

    text-align:center;

    gap:var(--journey-header-gap);

}

.eyebrow{

    display:inline-flex;

    align-items:center;

    justify-content:center;

    padding:
        .55rem
        1.25rem;

    border-radius:var(--radius-full);

    background:var(--surface);

    border:1px solid var(--border);

    color:var(--accent-light);

    font-size:var(--text-xs);

    font-weight:var(--font-semibold);

    letter-spacing:.22em;

    text-transform:uppercase;

    box-shadow:var(--shadow-soft);

}

.title{

    max-width:var(--journey-title-width);

    font-family:var(--font-display);

    font-size:var(--text-5xl);

    font-weight:var(--font-regular);

    line-height:.95;

    letter-spacing:var(--tracking-tight);

    color:var(--text-primary);

}

.title span{

    display:block;

    margin-top:.08em;

    background:linear-gradient(

        135deg,

        var(--ivory-50),

        var(--sand-300),

        var(--cocoa-500)

    );

    -webkit-background-clip:text;

    background-clip:text;

    -webkit-text-fill-color:transparent;

}

.divider{

    width:90px;

    height:1px;

    margin-block:.4rem .6rem;

    background:linear-gradient(

        90deg,

        transparent,

        var(--accent-light),

        transparent

    );

}

.description{

    max-width:var(--journey-description-width);

    color:var(--text-secondary);

    font-size:var(--text-lg);

    line-height:1.8;

}

</style>