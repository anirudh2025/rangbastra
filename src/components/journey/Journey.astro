---
import Section from "../primitives/Section.astro";
import Container from "../primitives/Container.astro";

import JourneyHeader from "./JourneyHeader.astro";
import JourneyTimeline from "./JourneyTimeline.astro";
---

<Section class="journey-section">
  <Container class="journey-container">
    <JourneyHeader />

    <JourneyTimeline />
  </Container>
</Section>

<style>
  :global(.journey-section) {
    --section-padding-block: var(--homepage-section-spacing);

    overflow: hidden;
    background:transparent;
  }

  :global(.journey-container) {
    --container-default: var(--container-2xl);
    --container-padding-inline: var(--section-padding-inline);

    position: relative;
    z-index: var(--z-content);
    display: flex;
    flex-direction: column;
    gap: clamp(var(--space-8), 7vw, var(--space-12));
  }

  @media (max-width: 767px) {
    :global(.journey-section) {
      --section-padding-block: var(--homepage-section-spacing);
    }

    :global(.journey-container) {
      --container-padding-inline: var(--section-padding-inline);
      gap: var(--space-8);
    }
  }
</style>
