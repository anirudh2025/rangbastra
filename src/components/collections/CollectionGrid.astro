---
import { collections } from "../../data/collections";
import CollectionCard from "./CollectionCard.astro";
---

<div class="collection-rail" aria-label="Featured collections">
  <ul class="collection-track">
    {
      collections.map((collection) => (
        <li class="collection-item">
          <CollectionCard {...collection} />
        </li>
      ))
    }
  </ul>
</div>

<style>
  .collection-rail {
    --collection-gap: clamp(var(--space-4), 1.8vw, var(--space-6));
    --collection-columns: 4;
    --collection-rail-padding: var(--space-1);

    width: 100%;
    max-width: 100%;
    overflow-x: auto;
    overflow-y: hidden;
    scroll-behavior: smooth;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
    overscroll-behavior-inline: contain;
    padding-inline: var(--collection-rail-padding);
    padding-block: var(--space-2) var(--space-7);
    contain: inline-size;
  }

  .collection-rail::-webkit-scrollbar {
    display: none;
  }

  .collection-track {
    display: grid;
    grid-auto-flow: column;
    grid-auto-columns: calc(
      (100% - (var(--collection-gap) * (var(--collection-columns) - 0))) /
        var(--collection-columns)
    );
    gap: var(--collection-gap);
    min-width: 100%;
  }

  .collection-item {
    min-width: 0;
    scroll-snap-align: start;
    scroll-snap-stop: always;
  }

  @media (max-width: 767px) {
    .collection-rail {
      --collection-columns: 1.18;
      --collection-gap: var(--space-5);
      --collection-rail-padding: var(--mobile-collection-gutter, var(--space-4));

      scroll-padding-inline: var(--collection-rail-padding);
      padding-block: var(--space-2) var(--space-6);
    }
  }

  @media (min-width: 768px) and (max-width: 1023px) {
    .collection-rail {
      --collection-columns: 2.35;
      --collection-gap: var(--space-5);
    }
  }

  @media (min-width: 1024px) {
    .collection-rail {
      --collection-columns: 4;
    }
  }
</style>
