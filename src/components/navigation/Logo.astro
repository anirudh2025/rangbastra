---
import logo from "../../assets/branding/RB Nav Logo.svg";

interface Props {
  alt?: string;
}

const { alt = "Rangbastra" } = Astro.props;
---

<img
  class="logo"
  src={logo.src}
  alt={alt}
  width="180"
  height="40"
  loading="eager"
  decoding="async"
  draggable="false"
/>
