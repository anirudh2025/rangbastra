---
import HeroContent from "./HeroContent.astro";
---

<section
    class="hero-desktop"
    aria-labelledby="hero-heading"
>

    <div class="hero-artwork">


    <img
        class="hero-image"
        src="https://res.cloudinary.com/dfxlm7z58/image/upload/v1782614896/Hero_Desktop_klovjb.webp"
        alt="Luxury Couture by Rangbastra"
        loading="eager"
        fetchpriority="high"
        decoding="async"
        draggable="false"
        width="4096"
        height="2731"
    />

</div>

    <div class="hero-overlay">

        <div class="hero-shell">

            <div class="hero-grid">

                <HeroContent />

            </div>

        </div>

    </div>

</section>

<style>
.hero-desktop{

    /* ==========================
       Layout Tokens
    ========================== */

    --hero-shell-width:min(100% - 9.5rem,1520px);

    --hero-content-width:clamp(44rem,48u3m9 l-w,52rem);

    --hero-content-offset:clamp(16rem,22vh,22rem);

    --hero-left:clamp(.5rem,1vw,1.25rem);

    position:relative;

    width:100%;

    overflow:hidden;

    isolation:isolate;

}

/* ========================================= */
/* Artwork */

.hero-artwork{

    display:block;

    width:100%;

    line-height:0;

}

.hero-image{

    display:block;

    width:100%;

    height:auto;

    user-select:none;

    pointer-events:none;

    -webkit-user-drag:none;

}

/* ========================================= */
/* Overlay */

.hero-overlay{

    position:absolute;

    inset:0;

    z-index:5;

}

/* ========================================= */

.hero-shell{

    width:var(--hero-shell-width);

    height:100%;

    margin-inline:auto;

}

/* ========================================= */

.hero-grid{

    display:grid;

    grid-template-columns:var(--hero-content-width);

    align-content:start;

    height:100%;

    padding-top:var(--hero-content-offset);

}

/* ========================================= */

.hero-grid > *{

    margin-left:var(--hero-left);

}

/* ========================================================= */
/* 1536+ */

@media (min-width:1536px){

.hero-desktop{

    --hero-content-width:52rem;

    --hero-content-offset:clamp(17rem,22vh,22rem);

}

}

/* ========================================================= */
/* Laptop */

@media (max-width:1440px){

.hero-desktop{

    --hero-shell-width:min(100% - 5rem,1320px);

    --hero-content-width:clamp(40rem,46vw,46rem);

    --hero-content-offset:clamp(14rem,20vh,17rem);

}

}

/* ========================================================= */
/* 1366 */

@media (max-width:1366px){

.hero-desktop{

    --hero-shell-width:min(100% - 8rem,1240px);

    --hero-content-width:clamp(38rem,48vw,43rem);

    --hero-content-offset:clamp(19.25rem,25vh,25.25rem);

}

}

/* ========================================================= */
/* Short Screens (IMPORTANT) */

@media (max-height:900px){

.hero-desktop{

    --hero-content-offset:clamp(18.25rem,22vh,24rem);

}

}

/* ========================================================= */

@media (max-height:820px){

.hero-desktop{

    --hero-content-offset:clamp(13.5rem,18vh,18.15rem);

}

}

/* ========================================================= */

@media (max-height:760px){

.hero-desktop{

    --hero-content-offset:12.5rem;

    --hero-content-width:clamp(34rem,45vw,40rem);

}

}

/* ========================================================= */
/* Tablet */

@media (max-width:1024px){

.hero-desktop{

    display:none;

}

}
</style>
